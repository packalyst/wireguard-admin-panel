# Traefik Static Configuration
api:
  dashboard: true
  insecure: true

# Local plugins
experimental:
  localPlugins:
    sentinel:
      moduleName: local/sentinel

entryPoints:
  web:
    address: ":${HTTP_PORT}"
    http:
      sanitizePath: false                    # ← stops the sanitizer mangling
      encodedcharacters:                     # ← explicitly allow encoded chars (defensive)
        allowencodedslash: true
        allowencodedbackslash: true
        allowencodednullcharacter: true
  websecure:
    address: ":${HTTPS_PORT}"
    http:
      sanitizePath: false                    # ← same
      encodedcharacters:
        allowencodedslash: true
        allowencodedbackslash: true
        allowencodednullcharacter: true
  traefik:
    address: ":${TRAEFIK_PORT}"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: INFO
  filePath: /var/log/traefik/traefik.log

accessLog:
  filePath: /var/log/traefik/access.log
  format: json
  fields:
    headers:
      defaultMode: drop
      names:
        User-Agent: keep
        X-Forwarded-For: keep
        X-Real-IP: keep
